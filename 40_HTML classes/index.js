// In this session we are gonna talk about on how to handle HTML classes and change it ?

// It is used for changing the previous class to the current class written in program
first.className = "text-dark color2"

// It is used for removing class
first.classList.remove("color2")

// It is used for adding class
first.classList.add("color1")

// It is used for continuous executions 
first.classList.toggle("color2")

// It is used to check that the class exist or not ?
first.classList.contains("text-dark")


