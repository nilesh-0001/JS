// In this session we are gonna talk about the Regular Expressions

const regex = /(Nishu){1}/gi

const text = "Nishu is a very very aweasome and very aweasome nice guy."
console.log(text.replace(regex, "VERY"))